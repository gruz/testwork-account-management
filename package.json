{
  "name": "account-management-backend-level-3",
  "version": "0.0.1",
  "scripts": {
    "build": "sudo apt-get install php-sqlite3 -y && cd laravel && touch database/database.sqlite && composer install",
    "start": "cd laravel && php artisan migrate && php artisan serve",
    "refresh": "cd laravel && php artisan migrate:refresh",
    "test": "cd laravel && php artisan migrate:refresh && cd ..; cypress run"
  },
  "devDependencies": {
    "cypress": "^5.4.0"
  }
}
