{
  "name": "account-management-backend-level-3",
  "version": "0.0.1",
  "scripts": {
    "build": "cd laravel; bin/init",
    "start": "cd laravel; php artisan serve",
    "test": "cd laravel; php artisan migrate:refresh; cd ..; cypress run"
  },
  "devDependencies": {
    "cypress": "^5.4.0"
  }
}
